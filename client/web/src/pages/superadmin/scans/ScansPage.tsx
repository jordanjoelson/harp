export default function ScansPage() {
  return (
    <div>
      Future create scan types maybe... will likely move this to the Admin scans
      but only allow super admins to create scan types
    </div>
  );
}
